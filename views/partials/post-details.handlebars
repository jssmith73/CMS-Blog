{{!-- <h2 class="title-headers">{{posts.title}}</h2>
<div id="post-body">
    <a href="/post/{{posts.id}}">
        <h3>{{posts.content}}</h3>
        <p id="author-date">{{format_date posts.date_created}}</p>
    </a>
</div> --}}

<h2 class="title-headers">{{title}}</h2>
<div id="post-body">
<h3>{{posts.content}}</h3>
<p id="author-date">{{posts.date_created}}</p>
</div>
<section class="comments">
    {{#each comments}}
    {{>comment-details}}
    {{/each}}
</section>